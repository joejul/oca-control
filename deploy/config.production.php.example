<?php
/**
 * Plantilla de config.php para produccion (Hostinger).
 * Copia este archivo a config.production.php (o directo a config.php en el
 * servidor), completa las credenciales reales y NO lo subas a git.
 */

return [
    'db' => [
        'host'    => 'localhost',
        'port'    => '3306',
        'name'    => 'uXXXXXXXX_ocacontrol',   // nombre de la base creada en hPanel
        'user'    => 'uXXXXXXXX_ocacontrol',   // usuario MySQL
        'pass'    => 'CAMBIAME',
        'charset' => 'utf8mb4',
    ],

    'timezone' => 'America/Costa_Rica',

    // Los archivos quedan en public_html/control/api/, y las peticiones llegan
    // como /control/api/...  -> hay que quitar ese prefijo antes de enrutar.
    'api_prefix' => '/control/api',

    // El front va en el mismo dominio: no hace falta CORS.
    'cors_origins' => [],

    'token_ttl_days'     => 30,
    'max_login_attempts' => 5,
    'lockout_minutes'    => 15,

    // Notificacion por correo al admin con cada solicitud nueva (horas extra / vacaciones).
    'mail' => [
        'host'      => 'CAMBIAME',   // ej. smtp.hostinger.com
        'port'      => 465,
        'username'  => 'CAMBIAME',   // ej. info@tudominio.com
        'password'  => 'CAMBIAME',
        'from'      => 'CAMBIAME',
        'from_name' => 'OCASO CAFE BAR',
        'admin_to'  => 'CAMBIAME',
    ],
];